# Quantum Feature Selection for Cotton Defoliation

**Paper:** *Quantum-Enhanced Feature Selection for UAV-Based Cotton Defoliation Assessment*  
**Repo:** [https://github.com/harshitha-8/Quantum-Feature-Selection](https://github.com/harshitha-8/Quantum-Feature-Selection)

---

## Project Structure

```
QuantumFeatureSelection/
├── scripts/                       ← Clean, citable Python scripts
│   └── evaluate_k_qubits.py       ← MAIN: full k=2,4,6 evaluation pipeline
│
├── results/                       ← All outputs (generated by scripts/)
│   ├── plots/                     ← Publication-quality 300 DPI PNG figures
│   ├── tables/                    ← CSV and text tables (for paper)
│   └── README.md                  ← Guide to each plot and table
│
├── icml_features_FULL.csv         ← Full dataset (1,549 UAV images × 12 features)
│
├── run_*.py                       ← Legacy individual plot scripts (kept for reference)
├── create_*.py                    ← Architecture diagram generators
│
└── .venv/                         ← Python virtual environment
```

---

## Reproducing the Main Results

```bash
# 1. Clone and navigate
git clone https://github.com/harshitha-8/Quantum-Feature-Selection.git
cd Quantum-Feature-Selection

# 2. Activate Python env (or install deps)
python -m venv .venv && source .venv/bin/activate
pip install scikit-learn pandas numpy matplotlib seaborn

# 3. Run the full evaluation
python scripts/evaluate_k_qubits.py

# Outputs → results/plots/ and results/tables/
```

---

## Key Results Summary

### Subset Definitions

| Method | k=2 | k=4 | k=6 |
|---|---|---|---|
| **QFS** (Quantum) | Std_ExG, Mean_RBR | + Mean_B, Correlation | + Mean_ExG, Mean_NGRDI |
| **MI** (Classical) | Std_ExG, Mean_ExG | + Mean_RBR, Mean_B | + Correlation, Mean_NGRDI |

### Performance Overview

| Condition | QFS k=6 | MI k=6 | QFS k=4 | MI k=4 |
|---|---|---|---|---|
| Clean data (1.0 is biologically expected) | 1.0000 | 1.0000 | 1.0000 | 1.0000 |
| Gaussian noise σ=0.10 | ~0.96 | ~0.95 | ~0.97 | ~0.95 |
| Gaussian noise σ=0.20 | ~0.85 | ~0.80 | ~0.88 | ~0.83 |
| Fog augmentation | 0.932 | 0.924 | 0.885 | 0.941 |
| Shadow augmentation | 0.585 | 0.585 | 0.585 | 0.997 |

> **Why 1.0 is correct:** The Excess Green Index (ExG) achieves AUC=1.0 as a single feature — Pre-defoliation fields are biologically lush green while Post-defoliation fields are brown/bare. The contribution of QFS is selecting the most *environmentally robust* combination of features.

---

## Figures for Paper

See [`results/README.md`](results/README.md) for a full index of all plots.

## CVPR Demo Builder

This repo now supports a CVPR-style demo package built around:

- normal pre-defoliation and post-defoliation UAV images
- generated heatmap visualizations for both states
- the 10 core plots from `results/plots/k_comparison`

Build the demo with:

```bash
python3 scripts/prepare_cvpr_demo_assets.py
python3 scripts/build_cvpr_demo.py
```

The CVPR demo manifest can also include a title card as the first slide. The current configuration opens with `Edge-AI: A Hybrid AI framework for Real-Time Vision at the Edge` and renders that title slide with Tahoma at 24 pt for the exported video frames.

Main repo files:

- `cvpr_demo_manifest.json`
- `scripts/prepare_cvpr_demo_assets.py`
- `scripts/build_cvpr_demo.py`

Generated outputs:

- `results/cvpr_demo/index.html`
- `results/cvpr_demo/storyboard.md`
- `results/cvpr_demo/cvpr_demo.mp4`
